{
  "name": "icecoder-desktop",
  "version": "1.0.1",
  "private": true,
  "description": "iceCoder Electron desktop shell (Windows / macOS / Linux)",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "icons:generate": "npm run desktop:icons --prefix ..",
    "dev": "npm run build && electron .",
    "pack": "npm run icons:generate && npm run build && electron-builder --dir",
    "dist:win": "npm run build && node scripts/dist-win.cjs",
    "dist:mac": "npm run icons:generate && npm run build && electron-builder --mac dmg",
    "dist:linux": "npm run icons:generate && npm run build && electron-builder --linux AppImage deb",
    "smoke:server-bundle": "node scripts/smoke-server-bundle.mjs",
    "rebuild:server-native": "node scripts/rebuild-server-native.cjs"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.7.2",
    "@types/node": "^25.6.0",
    "electron": "^32.2.5",
    "electron-builder": "^25.1.8",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "get-port": "^5.1.1",
    "iconv-lite": "^0.6.3"
  },
  "build": {
    "appId": "com.icecoder.app",
    "productName": "iceCoder",
    "asar": true,
    "icon": "assets/icon.png",
    "directories": {
      "output": "release",
      "buildResources": "assets"
    },
    "files": [
      "dist/**/*",
      "assets/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "server-bundle",
        "to": "server",
        "filter": [
          "**/*"
        ]
      }
    ],
    "asarUnpack": [
      "**/*.node",
      "**/node_modules/@vscode/ripgrep/**",
      "**/node_modules/node-pty/**"
    ],
    "afterPack": "scripts/stamp-win-icon.cjs",
    "win": {
      "target": [
        {
          "target": "nsis"
        }
      ],
      "icon": "assets/icon.ico",
      "artifactName": "iceCoder-windows.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "shortcutName": "iceCoder",
      "installerIcon": "assets/icon.ico",
      "uninstallerIcon": "assets/icon.ico",
      "installerHeaderIcon": "assets/icon.ico"
    },
    "mac": {
      "target": "dmg",
      "icon": "assets/icon.png",
      "category": "public.app-category.developer-tools"
    },
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ],
      "icon": "assets/icon.png",
      "category": "Development"
    }
  }
}
