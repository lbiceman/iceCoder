/** Legacy v1 field names. Import only for rejection/migration at compatibility boundaries. */
export const LEGACY_TASK_VERIFICATION_KEYS = [
  'verificationRequired',
  'verificationStatus',
] as const;
